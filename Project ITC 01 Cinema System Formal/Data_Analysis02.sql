USE Project_ITC_Cinema_System_Formal02;
Select * FROM customer;
# =========================================================================================================================================
# ========================================================= Update Info ===================================================================
# =========================================================================================================================================
# update info of 1st customer 
select * from customer;
UPDATE customer
	SET customerName = 'Kry Senghort', customerAge = 21, customerEmail = 'senghortkry@itc.edu.kh', customerPhone = '+1234567890'
		WHERE customerID = 1;

# update info of movieReleaseDate of 1st movie 
select * from movie;
Update movie 
	set movieReleaseDate = '2020-01-30',movieDuration = '02:00:00',movieRating = 8.6 
		where movieID = 1;
# =========================================================================================================================================
# ========================================================= Delete Info ===================================================================
# =========================================================================================================================================
DELETE FROM customer
	WHERE customerID = 53;

DELETE from movie 
	where movieID = 1;

DELETE from scheduled 
	where showMovieID = 1;
    
# =========================================================================================================================================
# ========================================================= Add column attribute in specific table ========================================
# =========================================================================================================================================
ALTER TABLE customer 
	ADD COLUMN customerAddress VARCHAR(100);

ALTER table theater
	add column theaterLocation varchar(50);
    

# =========================================================================================================================================
# ====================================================== Modify column attribute in specific table ========================================
# =========================================================================================================================================
# update dtype of feature 'customerAge' to float dtype
ALTER TABLE customer
	MODIFY COLUMN customerAge float;

# update dtype of feature 'customerName' to varchar(50) not null
ALTER TABLE customer
	MODIFY COLUMN customerName VARCHAR(50) NOT NULL;

# =========================================================================================================================================
# ====================================================== Modify column attribute in specific table ========================================
# =========================================================================================================================================
